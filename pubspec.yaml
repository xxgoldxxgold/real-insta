name: real_insta
description: "Real Insta - Instagram-like photo sharing app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.0.0
  go_router: ^14.0.0
  provider: ^6.0.0
  image_picker: ^1.0.0
  cached_network_image: ^3.3.0
  timeago: ^3.6.0
  flutter_cache_manager: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
